<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans.xsd
    http://code.alibabatech.com/schema/dubbo
    http://code.alibabatech.com/schema/dubbo/dubbo.xsd ">
    <!-- 消费方应用名，用于计算依赖关系，不是匹配条件，不要与提供方一样 -->
    <dubbo:application name="service-web-dubbo" />
    <!-- 使用multicast广播注册中心暴露发现服务地址 -->

    <!-- 福建连接地址 -->
    <!-- <dubbo:registry protocol="zookeeper" address="zookeeper://172.18.169.51:2181" 
        /> -->
    <!-- 北京连接地址 -->
    <dubbo:registry protocol="zookeeper"
        address="zookeeper://192.168.1.221:2181" />

    <!-- 生成远程服务代理，可以和本地bean一样使用demoService -->
    <dubbo:reference interface="com.bs.eips.user.service.UserService"
        id="eipsUserService" group="eips" />
    <dubbo:reference interface="com.bs.eips.sync.service.EInvoiceSyncService"
        id="eInvoiceSyncService" group="eips" timeout="10000" />
    <dubbo:reference
        interface="com.bs.eips.individualarchive.service.IndividualEIArchivePageService"
        id="individualEIArchivePageService" group="eips" />
    <dubbo:reference
        interface="com.bs.eips.individualarchive.service.IndividualEIArchiveService"
        id="individualEIArchiveService" group="eips" />

</beans>